% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{dist2knn}
\alias{dist2knn}
\title{dist2knn}
\usage{
dist2knn(embedding, k, mode = "connectivity", ...)
}
\arguments{
\item{embedding}{A distance matrix or `dist` object.}

\item{k}{Integer. Number of nearest neighbors to compute.}

\item{mode}{Character. Either `"dist"` or `"connectivity"` (default: `"connectivity"`).}

\item{...}{Additional arguments passed to `knn_flat()`.}
}
\value{
A sparse matrix representing the k-NN graph (class `dgCMatrix`).
}
\description{
Compute k-Nearest Neighbor Graph from Precomputed Distances
}
\details{
Given a distance matrix, constructs a k-nearest neighbor graph as a sparse matrix.
Optionally converts distances to Gaussian kernel similarities.
}
\examples{
dmat <- dist(matrix(rnorm(100), ncol = 2))
knn_graph <- dist2knn(dmat, k = 5)
print(knn_graph)

}
