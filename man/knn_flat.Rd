% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auxiliary.R
\name{knn_flat}
\alias{knn_flat}
\title{knn_flat}
\usage{
knn_flat(
  x,
  k,
  input = "matrix",
  symmetric = FALSE,
  if_dedup = FALSE,
  if_self = FALSE
)
}
\arguments{
\item{x}{A numeric matrix (rows = points) or a distance matrix.}

\item{k}{Integer. Number of neighbors to compute per point.}

\item{input}{Character. Either `"matrix"` or `"dist"` (default: `"matrix"`).}

\item{symmetric}{Logical. If `TRUE`, ensures bidirectional edges (default: `FALSE`).}

\item{if_dedup}{Logical. If `TRUE`, removes redundant (i,j)/(j,i) pairs (default: `FALSE`).}

\item{if_self}{Logical. If `TRUE`, includes self-edges (i,i) with distance 0 (default: `FALSE`).}
}
\value{
A data frame with columns `node1`, `node2`, and `dist`.
}
\description{
Compute Flattened k-Nearest Neighbor Edges
}
\details{
Computes a flattened list of k-nearest neighbor edges for each point in a matrix or distance matrix.
Returns a long-format data frame of neighbor pairs and their distances, with optional symmetry, self-edges, or deduplication.
}
\examples{
mat <- matrix(rnorm(100), ncol = 2)
edges <- knn_flat(mat, k = 5, symmetric = TRUE)
head(edges)

}
