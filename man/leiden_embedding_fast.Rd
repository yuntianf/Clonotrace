% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{leiden_embedding_fast}
\alias{leiden_embedding_fast}
\title{leiden_embedding_fast}
\usage{
leiden_embedding_fast(
  data,
  k = 30,
  prune.snn = 0,
  weight = "jaccard",
  resolution = 1
)
}
\arguments{
\item{data}{A numeric matrix of embeddings (e.g., PCA, UMAP).}

\item{k}{Integer. Number of nearest neighbors (default: 30).}

\item{prune.snn}{Numeric threshold to prune weak SNN edges (default: 0).}

\item{weight}{Character. Weighting scheme (currently unused; default: `"jaccard"`).}

\item{resolution}{Resolution parameter for Leiden clustering (default: 1).}
}
\value{
A factor vector of cluster labels.
}
\description{
Fast Leiden Clustering Using Sparse Shared Nearest Neighbors
}
\details{
Efficient implementation of SNN graph construction and Leiden clustering.
Uses matrix algebra for fast computation of shared neighbors and Jaccard similarity.
}
\examples{
mat <- matrix(rnorm(500), nrow = 100)
clusters <- leiden_embedding_fast(mat, k = 20)
table(clusters)

}
