% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_propagation.R
\name{label_spreading_bootstrap_blocked}
\alias{label_spreading_bootstrap_blocked}
\title{label_spreading_bootstrap_blocked}
\usage{
label_spreading_bootstrap_blocked(
  adj,
  labels,
  alpha = 0.8,
  sample_rate = 0.8,
  sample_n = 50,
  block_size = 128,
  tol = 0.001,
  max_iter = 100,
  epsilon = 0,
  refer_h5 = NULL,
  tmpdir = tempdir(),
  verbose = TRUE
)
}
\arguments{
\item{adj}{Sparse adjacency (\code{dgCMatrix}); will be row-normalized.}

\item{labels}{Vector of original labels (length \eqn{N}); mapped once to \code{1..C}.}

\item{alpha}{Damping parameter for label spreading.}

\item{sample_rate}{Fraction of labeled cells retained in each bootstrap.}

\item{sample_n}{Number of bootstrap replicates.}

\item{block_size}{Classes per block when streaming to HDF5.}

\item{tol, max_iter}{Convergence parameters.}

\item{epsilon}{Uniform prior mass added to avoid zeros.}

\item{refer_h5}{Optional existing reference HDF5 file; if \code{NULL}, computed internally.}

\item{tmpdir}{Directory for temporary HDF5 files.}

\item{verbose}{Logical; print progress.}
}
\value{
A list with:
\describe{
  \item{\code{prob}}{Dense reference \eqn{N \times C} probability matrix.}
  \item{\code{deviance}}{Per-cell mean L1 deviation vs reference.}
}
}
\description{
Bootstrap label spreading with HDF5 reference and per-cell deviance

Performs multiple label-spreading runs on random subsets of labeled cells,
compares each to a full reference solution, and returns the reference
probabilities plus mean L1 deviance per cell.
}
\details{
- Computes an exact reference with all labeled cells, normalizes it, and stores it in HDF5.
- Each bootstrap uses a random subset of labeled cells, writes its results to a temporary HDF5,
  and accumulates the L1 deviation vs the reference in streaming mode.
- Parallelizable via \pkg{future.apply}: set a plan before calling.
}
\examples{
\dontrun{
library(Matrix); library(future.apply)
plan(multisession, workers = 4)
N <- 1000; C <- 4
A <- rsparsematrix(N, N, 0.001); A <- abs(A) + Diagonal(N)
labs <- sample(c(1:C, NA), N, TRUE, prob = c(rep(0.05, C), 0.8))
res <- label_spreading_bootstrap_blocked(A, labs, sample_rate = 0.8, sample_n = 10)
str(res)
}
}
\seealso{
\code{\link{label_spreading_blocked}}, \code{\link{normalize_hdf5_rows}}
}
