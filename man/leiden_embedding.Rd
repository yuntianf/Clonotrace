% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{leiden_embedding}
\alias{leiden_embedding}
\title{leiden_embedding}
\usage{
leiden_embedding(
  data,
  k = 30,
  prune.snn = 0,
  weight = "jaccard",
  resolution = 1
)
}
\arguments{
\item{data}{A numeric matrix or data frame where rows are observations and columns are features.}

\item{k}{Integer. Number of nearest neighbors for SNN construction (default: 30).}

\item{prune.snn}{Numeric. Threshold below which SNN edges are removed (default: 0).}

\item{weight}{Character. Column to use as edge weight (`"jaccard"` or `"dis"`) (default: `"jaccard"`).}

\item{resolution}{Numeric resolution parameter for clustering (default: 1).}
}
\value{
A factor vector of cluster memberships for each row in `data`.
}
\description{
Run Leiden Clustering from Embedding via Shared Nearest Neighbors
}
\details{
Constructs a shared nearest neighbor (SNN) graph from an embedding matrix (e.g., PCA, UMAP),
optionally prunes weak connections, and performs Louvain or Leiden clustering.
}
\examples{
mat <- matrix(rnorm(500), nrow = 100)
clusters <- leiden_embedding(mat, k = 20)
table(clusters)

}
