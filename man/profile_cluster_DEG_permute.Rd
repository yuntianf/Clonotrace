% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile_DEG.R
\name{profile_cluster_DEG_permute}
\alias{profile_cluster_DEG_permute}
\title{profile_cluster_DEG_permute}
\usage{
profile_cluster_DEG_permute(P, G, dpt, n = 50)
}
\arguments{
\item{P}{A matrix of soft profile assignments for each cell (cells × 2, typically: target vs. control).}

\item{G}{A gene expression matrix (genes × cells).}

\item{dpt}{A numeric pseudotime vector (length = number of columns in `G`).}

\item{n}{Integer. Number of permutations (default: 50).}
}
\value{
A list of p-value vectors (one per permutation) representing the null distribution.
}
\description{
Permutation Null Distribution for Profile-Based Differential Expression
}
\details{
Computes a null distribution of p-values for profile-based differential gene expression
by repeatedly permuting the soft profile assignment matrix. This supports empirical
calibration of DE gene significance in pseudotime analysis.
}
\examples{
# Simulated example
G <- matrix(rnorm(5000), nrow = 100)
P <- matrix(runif(200), nrow = 100)
P <- cbind(target = P[, 1], control = 1 - P[, 1])
dpt <- seq(0, 1, length.out = 100)
null_pvals <- profile_cluster_DEG_permute(P, G, dpt, n = 10)
length(null_pvals)

}
