% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_propagation.R
\name{label_spreading_blocked}
\alias{label_spreading_blocked}
\title{label_spreading_blocked}
\usage{
label_spreading_blocked(
  adj,
  labels,
  label_n = NULL,
  alpha = 0.9,
  max_iter = 100,
  tol = 0.001,
  block_size = 128,
  outfile = NULL,
  verbose = TRUE,
  epsilon = 0
)
}
\arguments{
\item{adj}{\code{dgCMatrix} sparse adjacency; will be row-normalized
in-place to act as a transition matrix.}

\item{labels}{Integer or numeric vector of length \eqn{N} with class labels in
\code{1..C} and \code{NA} for unlabeled cells. Do not refactor.}

\item{label_n}{Optional total number of classes; inferred if \code{NULL}.}

\item{alpha}{Damping parameter in label propagation update.}

\item{max_iter}{Maximum iterations per block.}

\item{tol}{Convergence tolerance for updates.}

\item{block_size}{Number of classes per block to process at once.}

\item{outfile}{Optional HDF5 file path; if \code{NULL}, returns dense matrix.}

\item{verbose}{Logical; show progress messages.}

\item{epsilon}{Small uniform prior mass per class to avoid zeros.}
}
\value{
If \code{outfile = NULL}, a dense \eqn{N \times C} numeric matrix of
  class probabilities. Otherwise, invisibly returns the HDF5 file path with
  dataset \code{"prob"} written.
}
\description{
Memory-efficient label spreading (blocked, HDF5 optional)
Runs label spreading on a sparse graph in **class blocks** to control memory,
optionally streaming class probabilities to an on-disk HDF5 dataset.
}
\details{
- Normalizes the adjacency to a row-stochastic matrix.
- Splits the label space into \code{block_size}-sized groups to limit RAM use.
- Optionally writes each probability block directly into an HDF5 dataset.
}
\examples{
\dontrun{
library(Matrix)
set.seed(1)
N <- 1000; C <- 5
A <- rsparsematrix(N, N, 0.001); A <- abs(A) + Diagonal(N)
labs <- sample(c(1:C, NA), N, TRUE, prob = c(rep(0.05, C), 0.75))
P <- label_spreading_blocked(A, labs, label_n = C, block_size = 2, max_iter = 50)
}
}
\seealso{
\code{\link{create_hdf5_matrix}}, \code{\link{normalize_hdf5_rows}},
  \code{\link{label_spreading_bootstrap_blocked}}
}
